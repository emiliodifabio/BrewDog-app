{
  "name": "brewery-app",
  "version": "1.0.0",
  "description": "Brewery App",
  "default": "index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "targets": {
    "main": false
  },
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --dist-dir ./dist"
  },
  "author": "Emilio Di Fabio",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.7.0"
  }
}
